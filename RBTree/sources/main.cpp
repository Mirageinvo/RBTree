#include <iostream>
#include "../includes/rbtree.hpp"

int main() {
    char sign;
    RBTree<int> tr;
    while (!std::cin.eof()) {
        std::cin >> sign;
        if (sign == 'k') {
            
        }
        else if (sign == 'm') {

        }
        else if (sign == 'n') {

        }
        else {
            std::cout << "Unknown command" << std::endl;
        }
    }
    return 0;
}