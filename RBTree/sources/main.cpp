#include <iostream>
#include "rbtree_impl.hpp"

int main() {
    char sign;
    Trees::RBTree<int> tr;
    std::cin >> sign;
    /*while (!std::cin.eof()) {
        if (sign == 'k') {
            
        }
        else if (sign == 'm') {

        }
        else if (sign == 'n') {

        }
        else {
            std::cout << "Unknown command" << std::endl;
        }
        std::cin >> sign;
    }*/
    return 0;
}